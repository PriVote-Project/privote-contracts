export enum AuthType {
  FREE = "free",
  ANON = "anon",
}

export enum PollType {
  SINGLE = "single",
  MULTI = "multi",
}

export enum ContractPollType {
  NOT_SELECTED,
  SINGLE_VOTE,
  MULTIPLE_VOTE,
  WEIGHTED_MULTIPLE_VOTE,
}
